!/bin/bash

Us=$(id -u)

if [ $Us -ne 0 ]; then
        echo "you need to be a root user to excute this script"
        exit 1
fi
apt install git -y
if [ $? -ne 0 ]; then 
        echo "git is failure "
else
        echo "git is success"
fi
